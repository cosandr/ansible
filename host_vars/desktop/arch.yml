---

kernels:
  - "linux-lts"
  - "linux-zen"

mkinitcpio_no_fallback: "{{ kernels }}"

network_manager: "systemd-networkd"
networkd_configs:
  - name: wired
    match: 'Name=en*'
    network: 'DHCP=yes'

boot_loader: "refind"
