---

__subject_alt_name:
  - "DNS:{{ domain }}"
  - "DNS:{{ wireguard_ip }}"

ca_path: "/usr/local/share/ca-certificates/elk-ca.crt"
self_signed_ssl:
  - name: filebeat
    dest: "/etc/filebeat"
    size: 2048
    format: "pkcs8"
    subject_alt_name: "{{ __subject_alt_name }}"
