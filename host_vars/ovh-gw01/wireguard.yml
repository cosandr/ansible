---
wireguard_port: "51820"
wireguard_interface: "gw01"
wireguard_address: "172.16.1.1/24"
wireguard_endpoint: "{{ domain }}"
wireguard_persistent_keepalive: "30"

wireguard_ip: "{{ wireguard_address.split('/')[0] }}"
