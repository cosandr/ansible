---
_ports:
  adminer: 54322
  cadvisor: 8081
  gitea: 3000
  grafana: 3001
  nginx_exporter: 9113
  postgres: 5432
  prometheus: 9092
  psql_exporter: 9187
  rtmp: 1935
  vouch: 8682
borg_remote_passphrase: "{{ vault_borg_remote_passphrase }}"
borg_server_passphrase: "{{ vault_borg_server_passphrase }}"
container_data: "/srv/containers"
postgres_service_name: postgresql-13.service
rtv_ddns_domain: "{{ vault_rtv_ddns_domain }}"
rtv_domain: "{{ vault_rtv_domain }}"
rtv_scrape_pass: "{{ vault_rtv_scrape_pass }}"
rtv_scrape_user: "{{ vault_rtv_scrape_user }}"
rtv_ssh_port: "{{ vault_rtv_ssh_port }}"
rtv_syncthing_port: "{{ vault_rtv_syncthing_port }}"
server_domain: "dresrv.com"
server_hostname: DreSRV
smtp_host: "{{ vault_smtp_host }}"
smtp_pass: "{{ vault_smtp_pass }}"
smtp_port: "{{ vault_smtp_port }}"
smtp_user: "{{ vault_smtp_user }}"
web_root: "/var/www"
pip_wheel_dir: "{{ web_root }}/pip"
