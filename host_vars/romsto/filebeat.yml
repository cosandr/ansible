---

filebeat_config:
  filebeat.inputs:
    - type: docker
      containers.ids: '*'
      tags: ['docker']

    - type: log
      paths: ['/var/log/syslog']
      tags: ['syslog']

  output.elasticsearch:
    hosts: "{{ vault_es_hosts }}"
    api_key: "{{ vault_filebeat_key }}"
