---

- hosts: webgw, romsto, desktop, laptops
  gather_facts: false
  pre_tasks:
    - name: Run common role
      import_role:
        name: common
      tags: ["always"]

    - name: Gather facts
      setup:
      tags: ["always"]
  roles:
    - role: wireguard
      become: true
