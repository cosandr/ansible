#!/usr/bin/env -S ansible-playbook
---
- hosts: desktop, dresrv
  gather_facts: false
  roles:
    - role: common
      tags: ["always"]

- hosts: desktop, dresrv
  gather_facts: true
  pre_tasks:
    - name: Merge ports
      set_fact:
        ports: "{{ ports | combine(_ports, recursive=true) }}"
      tags: ["always"]
  roles:
    - role: borg
      tags: ["borg"]

    - role: btrbk
      tags: ["btrbk"]
