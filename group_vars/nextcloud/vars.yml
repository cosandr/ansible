---

provisioned_disks:
  - dev: "/dev/sdb"
    vols:
      - name: nextcloud
        path: "/var/www/html/nextcloud"
        shrink: false
        size: "100%FREE"

nextcloud_port: 443
nextcloud_proxies: "{{ proxy_hosts }}"
nextcloud_domain: "nextcloud.{{ hostvars['webgw01'].domains[1] }}"
nextcloud_server_name: "{{ nextcloud_domain }}"
