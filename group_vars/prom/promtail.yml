---

promtail_syslog_port: 514

__promtail_host_configs:
  - job_name: syslog
    syslog:
      listen_address: "0.0.0.0:{{ promtail_syslog_port }}"
      labels:
        job: "syslog"
    relabel_configs:
      - source_labels: ['__syslog_message_hostname']
        target_label: 'host'
