---

proxy_hosts:
  - "{{ hostvars['webgw01'].wireguard_ip }}"
  - "{{ hostvars['localgw01'].ansible_host }}"

smartd_email: "{{ my_email }}"

smtp_config: "{{ vault_smtp_config }}"
# smtp_config:
#   example.com:
#     host:
#     pass:
#     port:
#     user:

# Fetch with timedatectl -p Timezone --value show
sys_timezone: "Europe/Oslo"

ssh_authorized_keys:
  - "{{ hostvars['desktop'].ssh_keys.andrei }}"
  - "{{ hostvars['laptop'].ssh_keys.andrei }}"

domain: "vm.{{ domains['hb'] }}"
