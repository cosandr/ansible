---

proxy_hosts:
  - "{{ hostvars['webgw01'].wireguard_ip }}"
  - "{{ hostvars['localgw01'].ansible_host }}"

smtp_config: "{{ vault_smtp_config }}"
# smtp_config:
#   example.com:
#     host:
#     pass:
#     port:
#     user:

firewall_frontend: 'firewalld'
node_exporter_config_firewall: true
node_exporter_allow_ip: "{{ hostvars['prom01'].ansible_host }}"
