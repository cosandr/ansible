---

mt_bridges:
  - name: bridge1
    frame-types: admit-only-vlan-tagged
    vlan-filtering: true

mt_bridge_ports: "{{ mt_trunk_ports + mt_access_ports | map(attribute='ports') | flatten | sort }}"
