---

__nginx_upstreams_host:
  - name: syncthing
    servers: ["{{ hostvars['theia'].host_ips.vm }}:8384"]

  - name: motion
    servers: ["{{ hostvars['drepi'].ansible_host }}:{{ hostvars['drepi'].motion_port }}"]

__nginx_vhosts_host:
  - filename: syncthing.conf
    server_name: syncthing
    upstream_name: syncthing
    template: "{{ repo_base_dir }}/files/webgw/nginx/syncthing.conf.j2"
