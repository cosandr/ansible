---

__mt_dns_entries_group: []
__mt_dns_entries_host:
  - name: "{{ inventory_hostname }}.{{ domains['hb'] }}"
    address: "{{ host_ips.general }}"
  - name: "{{ inventory_hostname }}.{{ all_net.mgmt.domain }}"
    address: "{{ host_ips.mgmt }}"
  - name: "{{ inventory_hostname }}.{{ all_net.san.domain }}"
    address: "{{ host_ips.san }}"
  - name: "{{ inventory_hostname }}.{{ all_net.vm.domain }}"
    address: "{{ host_ips.vm }}"
  - name: "{{ inventory_hostname }}.ipmi.{{ domains['hb'] }}"
    address: "{{ host_ips.ipmi }}"
