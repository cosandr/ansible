---

root_disk: "/dev/disk/by-id/ata-SAMSUNG_MZ7LH960HAJR-00003_S4F4NA0M701202"

web_root: "/var/www"
pip_wheel_dir: "{{ web_root }}/pip"

btrfs_subvols:
  - "@snapshots"
  - "@/var/lib/libvirt/images"
  - "@/mnt/kube"
  - "@home/{{ my_user }}"

prometheus_node_targets: ["{{ host_ips.vm + ':' + node_exporter_port | string }}"]

__unattended_package_blacklist_4:
  - ".*zfs.*"
  - ".*zpool.*"

grub_cmdline_group: "vfio-pci.ids=10de:2204,10de:1aef amd_pstate=passive"

ssh_conf_hosts: false

syncthing_gui_listen_address: "{{ host_ips.general }}:8384"
