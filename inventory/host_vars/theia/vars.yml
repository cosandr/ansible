---

domain: "{{ domains['hb'] }}"

cpupower_start_governor: powersave
cpupower_stop_governor: ondemand

web_root: "/var/www"
pip_wheel_dir: "{{ web_root }}/pip"

btrfs_scrub_targets:
  - /
  - /mnt/tank

prometheus_node_targets: ["{{ host_ips.vm + ':' + node_exporter_port | string }}"]
