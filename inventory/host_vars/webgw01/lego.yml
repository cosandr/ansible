---

__host_lego_deploy_hooks:
  - name: sudo-restart-postfix
    content: |
      #!/bin/sh

      [ "$LEGO_HOOK_CERT_NAME" = "{{ postfix_hostname | community.dns.get_registrable_domain }}" ] || exit 0
      systemctl restart postfix
