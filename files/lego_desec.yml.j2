{% if names is not defined %}
name:
  - "{{ domains[key] }}"
  - "*.{{ domains[key] }}"
{% else %}
name: {{ names | to_json }}
{% endif %}
email: "{{ email | default(my_email_alt) }}"
dns: desec
env:
  DESEC_TOKEN: "{{ lego_desec_token }}"
