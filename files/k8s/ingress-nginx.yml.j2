---

controller:
  watchIngressWithoutClass: true
  ingressClassByName: true
  kind: DaemonSet
  service:
    externalTrafficPolicy: Local
    type: LoadBalancer
    loadBalancerIP: "{{ vips.kube.nginx | ansible.utils.ipv4 | first }}"
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
