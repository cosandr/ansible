{{ ansible_managed | comment }}
[Unit]
Description=Run Sanoid Every {{ sanoid_timer_minutes }} Minutes
Requires=sanoid.service

[Timer]
OnCalendar=*:0/{{ sanoid_timer_minutes }}
Persistent=true

[Install]
WantedBy=timers.target
